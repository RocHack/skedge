#splash-space

#splash
  %a.enormous{href:"./"}
    skedge
  .sub
    a scheduling system for the u of r

.splash-hr
%hr.half
.splash-container
  .row
    .splash-options
      .col-md-4.lead
        %a{href:"#depts", onclick:"splashtoggle('#departments');"} departments
      .col-md-4.lead
        %a{href:"#", onclick:"$('#search-input').focus(); return false;"} search
      .col-md-4.lead
        %a{href:"#about", onclick:"splashtoggle('#info');"} about

#info
  .row
    %p.info-text
      %strong skedge
      is a student-made alternative to the official
      <a href="https://cdcs.ur.rochester.edu/">cdcs</a>.
      here are some things that it offers:
    %ul.info-text
      %li simplified search
      %li grouped course sections
      %li labs, recitations, and workshops embedded in courses
      %li clickable course mentions & instructors 
      %li easy, live-updating schedule sharing
      %li & more coming soon...
      %li.text-danger
        please remember that you cannot register for courses using skedge
        %strong (!)
      /%li{style:"color:gray"}
      /%a{href:"#", onclick:"$(this).parent().hide(); $('.more').show(); return false;"} 
        
      .more
        %li desired courses -> different schedule options
        %li autocomplete search w/ add btn in autocomplete box
        %li compare yr schedule with a friend's & see mutual free time      

    .credits
      %p
        a
        %a{href:"http://rochack.org"} rochack
        project
      %p
        made by
        %a{href:"http://danhassin.com"} dan
      %p
        =bracket_link("source", "https://github.com/RocHack/skedge")
        =bracket_link("feedback", "#", onclick:"prepareModal(); return false;", data:{toggle:"modal", target:"#bug-modal"})
        =render partial:"ticket"

    %p.info-text{style:"margin-top:20px;"}
      %a{href:"#", onclick:"$('#transfer-stuff').show(); $(this).hide(); return false;"}
        using skedge across computers or browsers?
    
    #transfer-stuff{style:"display:none"}
      %p.info-text
        -key=nil
        -if s=cookies['s_id']
          -key=s.split("&")[1]
          -if key && !key.empty?
            %div{style:"margin-bottom:10px"}
              %strong this account's key is:
              =key
            
        %strong
          =key ? "change" : "set"
          your account key:
        %input.form-control#transfer{style:"width:300px; display:inline-block; margin-left:2px;"}
        %button.btn.btn-default{onclick:"transfer();"} go

#departments.cols
  -@depts.each do |dept|
    -if true#!dept.courses.empty?
      .dept
        %a
          %strong
            %a{href:"/?q=#{dept.short}"}= dept.short
        ="-"
        =dept.name

:javascript
  var hash = window.location.hash;
  if (hash == "#about")
    splashtoggle('#info');
  else if (hash == "#depts")
    splashtoggle('#departments');

